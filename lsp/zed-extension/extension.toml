[extension]
id = "set-trace"
name = "set-trace"
description = "Claim traceability visualization — coverage diagnostics, navigation, code lens"
version = "0.2.0"
authors = ["set-trace contributors"]

[language_servers.set-trace]
name = "set-trace"
languages = ["Markdown"]
