[extension]
id = "tracecart"
name = "tracecart"
description = "Claim traceability visualization — coverage diagnostics, navigation, code lens"
version = "0.2.0"
authors = ["tracecart contributors"]

[language_servers.tracecart]
name = "tracecart"
languages = ["Markdown"]
