You are extracting atomic requirements from a Hungarian specification document.
The text has been pre-split into numbered clauses. Each clause has a clause_id (e.g., L27-C3).

RULES:
- Every clause that carries meaning must produce at least one requirement entry
- If a clause contains multiple independent facts joined by "vagy" (or), split them
- IMPLICIT requirements — extract ONLY when the source text LINGUISTICALLY implies them:
  - Modal verbs: "küldhet" (may) ≠ "küld" (does) → note optionality
  - Scope markers: "kizárólag" → hard boundary that needs enforcement
  - Negation scope: "nem vonatkozik X-re" → exclusion boundary needs category logic
  - Domain terms: "B2B" → implies not end-consumer
  DO NOT extract implicit requirements based on logical inference or implementation guessing:
  - BAD: "figyelembe veszi a raktárkészletet" → "valós idejű raktáradat szükséges" (implementation guess)
  - BAD: "minimális rendelési mennyiség" → "konfigurálhatónak kell lennie" (logical inference)
  - GOOD: "kizárólag FSC beszállítóktól" → "a rendszernek meg kell akadályoznia nem-FSC rendelést" (linguistic: "kizárólag" = enforcement)
  The test: could a reader DISAGREE with the implicit requirement while agreeing with the source text? If yes, don't extract it.
- NEGATIVE requirements: "nem lesz", "nincs szükség", "nem cél" → type: EXCLUSION
- Keep ORIGINAL Hungarian text — do not translate
- Tag each entry:
  - type: REQUIREMENT | DECISION | WISH | OPEN_QUESTION | EXCLUSION
  - tags (one or more): orders, email, ai, materials, manufacturing, logistics, communication, crm, partners, billing, products, finishing, capacity, shipping, documents, design, portal, pricing

OUTPUT FORMAT — JSON array only, no other text:
[
  {
    "id": "CS-{chapter}-{NNN}",
    "clause_id": "L27-C3",
    "type": "REQUIREMENT",
    "tags": ["orders", "manufacturing"],
    "text": "A visszaigazolásnak figyelembe kell vennie a CNC kapacitást",
    "triple": {"subject": "visszaigazolás", "attribute": "figyelembe veszi", "object": "CNC kapacitás"},
    "implicit": false
  }
]

For implicit requirements, set "implicit": true and add "signal" field explaining what word/phrase triggered it.

CLAUSE LIST:
{clauses}

AFTER extraction, CHECK: list any clause_ids from the input that are NOT cited by any requirement. These are potential gaps — extract requirements from them too.
